# v1.3.0 GitHub 发布材料

目标仓库：`SevChu/Snooker`，分支：`main`。此文件用于发布执行；准备完成不代表已经推送或公开发布。

## 发布元数据

- Git tag：`v1.3.0`，指向包含本次代码、测试、版本与文档的发布提交。
- 建议提交标题：`Release Snooker v1.3.0 with advanced statistics and pocket fixes`
- Release 标题：`Snooker v1.3.0 — 高阶数据、袋口修正与超分认输`
- Release 正文：仓库根目录 `RELEASE_NOTES.md`，与离线 ZIP 中的说明一致，包含 v1.4.0 优化人机对战逻辑的预告。
- 发布类型：正式版本，设为 Latest，不标记为 prerelease。
- About 描述：`Snooker v1.3.0 · 离线斯诺克游戏，支持 Frame / Match 高阶统计、等宽袋口与超分认输。v1.4.0 将优化人机对战逻辑。`
- README 标题保持 `Snooker`；保留仓库名称、主页地址、主题和历史 Release，不移动历史标签。

## 发布附件

仅上传以下本次生成的文件，避免混入 release 目录内的历史附件：

1. `release/Snooker-v1.3.0.html`
2. `release/Snooker-v1.3.0.zip`
3. `release/SHA256SUMS.txt`

ZIP 内含游戏 HTML、游玩说明、RELEASE_NOTES.md、LICENSE 和 THIRD_PARTY_NOTICES.txt。源码提交不包含被忽略的 `release/`、`dist/` 和 `node_modules/`；构建附件通过 GitHub Release 分发。

## 发布顺序与验收

1. 执行 `npm test`、`npm run typecheck`、`npm run build` 和 `npm run build:standalone`，确认 147 项测试通过。
2. 核对 package.json 与 lockfile 的项目版本、主页版本、发布说明、第三方声明和附件文件名均为 v1.3.0。v1.2.0 发布说明已归档至 `docs/releases/v1.2.0.md`。
3. 核对 ZIP 内五个文件、内嵌许可、HTML 离线依赖检查，以及 SHA256SUMS.txt 中的实际摘要。
4. 检查远端 main 与 v1.3.0 标签是否已有更新；如有新提交，先整合并重新验证，不强制推送或覆盖已有标签。
5. 审查并提交本次变更，推送发布提交与对应 v1.3.0 标签到 origin。
6. 使用该标签创建正式 Release，以根目录 RELEASE_NOTES.md 为正文，附上以上三个文件并设为 Latest。多行正文使用文件传入，例如 gh 的 `--notes-file RELEASE_NOTES.md`。
7. 更新仓库 About 描述；检查 Release 标签对应提交、Latest 状态、附件、README 下载链接与简介。
8. 下载发布附件并比对 SHA-256，确认公开附件与本地构建一致。

计划发布地址：`https://github.com/SevChu/Snooker/releases/tag/v1.3.0`。公开发布前该地址及 README 中 v1.3.0 下载链接可能尚不可用。

## 本次准备记录（2026-09-14）

- 147 项测试、TypeScript 检查、常规构建与离线构建均通过。
- 已逐项比对 ZIP 内五个文件与构建输出，HTML / ZIP 的 SHA-256 均与校验文件一致。
- GitHub API 检查：远端 main 为 `276172a972b6c642769b977d1f808b4df54459de`，与准备时本地 HEAD 一致；尚无 v1.3.0 标签。正式推送前需重新检查远端状态。
- 准备阶段未创建发布提交、标签或 GitHub Release；后续按上述发布顺序执行，并以 GitHub 上的实际状态为准。
- 本地旧版 HTML / ZIP 及旧版解压校验目录已清理，保留 v1.3.0 附件、历史源码文档和规则核对资料。
